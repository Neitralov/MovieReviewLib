@inject NavigationManager NavigationManager

<header class="bg-neutral-900 h-[100px] flex sticky top-0">
    <h1 class="text-[32px] font-bold text-white my-[30px] mx-[50px] inline-block leading-[40px]">MovieReviewLib</h1>
    <input class="h-[50px] my-[25px] bg-neutral-800 text-neutral-100 placeholder-neutral-400 w-full indent-[20px] rounded-[5px] text-[16px] bold" type="text" placeholder="Введите название фильма..." @bind="Name" @bind:event="oninput" @onkeydown="FindMovie"/>
    <button class="bg-emerald-600 hover:bg-emerald-700 text-white rounded-[5px] cursor-pointer m-[25px] mr-[50px] text-[24px] font-bold px-[40px]" @onclick="OpenMovieEditor">Добавить</button>
</header>

@code {
    private string? Name { get; set; }

    private void FindMovie(KeyboardEventArgs args)
    {
        if (args.Code is not "Enter")
            return;

        NavigationManager.NavigateTo(string.IsNullOrWhiteSpace(Name) ? "/" : $"/FoundMovies/?Name={Name}");
    } 
    
    private void OpenMovieEditor() => NavigationManager.NavigateTo("/Movie");
}